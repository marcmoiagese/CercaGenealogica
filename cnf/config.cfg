#mysql, postgres
DB_PATH=./database.db

# sqlite | postgres | mysql
DB_ENGINE=sqlite

RECREADB=true           # Executa el fitxer sql sobre la base de dades al iniciar l'aplicació (per crear les taules, etc.)
RECREADB_RESET=false    # Elimina i recrea la base de dades cada vegada que s'inicia l'aplicació (per desenvolupament, no recomanat en producció)
REGISTERD=truea

# Enviament de correus (activació, etc.)
MAIL_ENABLED=true
MAIL_FROM=no-reply@cercagenealogica.local
MAIL_SMTP_HOST=localhost
MAIL_SMTP_PORT=25

# Opcional: si s'utilitza Postgres/MySQL
DB_HOST=devstack.marc.cat
DB_USR=recerca
DB_PASS=d3l$4b4n7p4$$a7$
# postgres:5432 / mysql:3306
DB_PORT=3306
DB_NAME=cerca_genealogica


###
# LOG_LEVEL: determina el grau de verbosity
#   Valors suportats:
#   - silent/error: només errors greus
#   - info: events generals (per defecte)
#   - debug: detalls extensos per diagnosi
###
LOG_LEVEL=debug

# Espai personal - Gramps integration
# ESP_GRAMPS_SECRET can be raw text or "base64:..." (min 32 bytes after decode)
ESP_GRAMPS_SECRET=dev-change-me
ESP_GRAMPS_SYNC_INTERVAL_MINUTES=60
ESP_GRAMPS_SYNC_BACKOFF_MINUTES=5
ESP_GRAMPS_HTTP_TIMEOUT_SECONDS=20

# Media
MEDIA_ENABLED=true
MEDIA_ROOT=./data/media
MEDIA_MAX_UPLOAD_MB=200
MEDIA_ALLOWED_MIME=image/jpeg,image/png,image/tiff

# Media credits/grants
MEDIA_GRANT_HOURS=24
MEDIA_POINTS_BASE=10
MEDIA_POINTS_K=2
MEDIA_POINTS_PER_CREDIT=10
